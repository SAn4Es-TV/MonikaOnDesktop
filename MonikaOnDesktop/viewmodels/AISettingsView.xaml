<UserControl
    x:Class="MonikaOnDesktop.AISettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MonikaOnDesktop"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="336"
    d:DesignWidth="510"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <WrapPanel Margin="0,3,0,0">
                            <Image
                                x:Name="img"
                                Width="17"
                                Source="/images/check_selected_foreground.png"
                                Visibility="Hidden" />
                            <TextBlock
                                x:Name="buttText"
                                Margin="0,0,0,0"
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource halogen}"
                                FontSize="17"
                                Foreground="#FFA5A5A5"
                                Text="{TemplateBinding Content}" />
                        </WrapPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="buttText" Property="Foreground" Value="#FFBB5599" />
                                <Setter TargetName="img" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttText" Property="Foreground" Value="#FFBB5599" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>

            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Image RenderOptions.BitmapScalingMode="NearestNeighbor" Source="/images/horizontal_hover_thumb.png" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarBtn" TargetType="{x:Type RepeatButton}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Grid.Background>
                                <ImageBrush ImageSource="/images/horizontal_poem_bar.png" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                            </Grid.Background>
                            <Track
                                x:Name="PART_Track"
                                IsDirectionReversed="true"
                                ViewportSize="NaN">

                                <Track.DecreaseRepeatButton>
                                    <RepeatButton
                                        Margin="2"
                                        Command="ScrollBar.PageUpCommand"
                                        Opacity="1"
                                        Style="{StaticResource ScrollBarBtn}" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton
                                        Margin="2"
                                        Command="ScrollBar.PageDownCommand"
                                        Opacity="1"
                                        Style="{StaticResource ScrollBarBtn}" />
                                </Track.IncreaseRepeatButton>

                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="sub" TargetType="Button">
            <Setter Property="Background" Value="Aqua" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="#01ffffff">
                            <local:OutlinedTextBlock
                                x:Name="buttText"
                                Margin="0,0,0,0"
                                Fill="White"
                                FontFamily="{StaticResource riffic}"
                                FontSize="24"
                                Stroke="#FFBB5599"
                                StrokeThickness="2.5"
                                Text="{TemplateBinding Content}" />
                            <local:OutlinedTextBlock
                                x:Name="buttText1"
                                Margin="0,0,0,0"
                                Fill="White"
                                FontFamily="{StaticResource riffic}"
                                FontSize="24"
                                Stroke="#FFBB5599"
                                StrokeThickness="0"
                                Text="{TemplateBinding Content}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="Transparent">
        <WrapPanel Margin="20">
            <StackPanel Margin="0,0,70,0">
                <Button Content="{DynamicResource loc_Cat_Main}" Style="{StaticResource sub}" />
                <CheckBox Content="{DynamicResource loc_Chk_AI_Enable}" IsChecked="{Binding Settings.AI}" />
            </StackPanel>
            <StackPanel Margin="0,0,0,0">
                <Button Content="{DynamicResource loc_Cat_Token}" Style="{StaticResource sub}" />
                <Border Padding="1" BorderThickness="0">
                    <Border.Background>
                        <ImageBrush ImageSource="/images/textbox_background.png" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                    </Border.Background>

                    <TextBox
                        Width="150"
                        Background="{x:Null}"
                        BorderBrush="{x:Null}"
                        BorderThickness="0"
                        FontFamily="{StaticResource halogen}"
                        FontSize="17"
                        Foreground="#FFA5A5A5"
                        SelectionBrush="Transparent"
                        SelectionTextBrush="#FFBB5599"
                        Text="{Binding Settings.token}" />

                </Border>
                <Button
                    Margin="0,10,0,0"
                    Content="{DynamicResource loc_Cat_Port}"
                    Style="{StaticResource sub}" />
                <Border Padding="1" BorderThickness="0">
                    <Border.Background>
                        <ImageBrush ImageSource="/images/textbox_background.png" RenderOptions.BitmapScalingMode="NearestNeighbor" />
                    </Border.Background>

                    <TextBox
                        Width="150"
                        Background="{x:Null}"
                        BorderBrush="{x:Null}"
                        BorderThickness="0"
                        FontFamily="{StaticResource halogen}"
                        FontSize="17"
                        Foreground="#FFA5A5A5"
                        SelectionBrush="Transparent"
                        SelectionTextBrush="#FFBB5599"
                        Text="{Binding Settings.port}" />

                </Border>
            </StackPanel>
        </WrapPanel>
    </Grid>
</UserControl>
